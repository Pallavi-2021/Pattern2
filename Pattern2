#include <stdio.h>

int main (void)
{
    int n;
    printf("Enter the number of stairs:");
    scanf("%d",&n);
    for(int i=0 ; i<n; i++)
    {
        for (int j=0; j<=i; j++)
        {
            printf("01");
        }
        printf(" ");
    }
    return 0;


}
